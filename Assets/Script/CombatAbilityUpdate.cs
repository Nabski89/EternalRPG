using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CombatAbilityUpdate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }




    //click on the button

    //each button has has the character number and equipment slot checked
    //check character based on skill
    //enable the targeted skill thing


    // the button for that selects that as the skill and closes this menu
}
