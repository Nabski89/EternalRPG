using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyUIBarController : MonoBehaviour
{
public 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //updates the health bar when they take damage, updates the wait bar while waiting for them to get ready to take their turn
    //if I'm feeling kind I'd give any kind of indication on what the other stats are, probably from a drop down menu
    //maybe some kind of drop indication too
}
